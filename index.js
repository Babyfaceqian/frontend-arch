! function () {
  /** 
   * d3.now()
   */
  console.log('d3.now():', d3.now());

  /**
   * d3.timer(callback[, delay[, time]]) 
   */

  /**
   * timer.restart(callback[, delay[, time]])
   */

  /**
   * timer.stop()
   */

  /**
   * d3.timerFlush()
   */

  /**
   * d3.timeout(callback[, delay[, time]])
   */

  /**
   * d3.interval(callback[, delay[, time]]) 
   */
}()